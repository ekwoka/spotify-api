{
  "name": "@ekwoka/spotify-api",
  "author": "Eric Kwoka",
  "license": "MIT",
  "version": "0.0.1",
  "description": "Composable Wrapper for the Spotify Web Api and Spotify Web Playback SDK",
  "keywords": [
    "spotify",
    "api",
    "wrapper",
    "composable",
    "playback",
    "typescript",
    "node"
  ],
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node scripts/esbuild.js",
    "build:tsc": "tsc",
    "lint": "eslint --fix .; prettier --write . --loglevel error",
    "patch": "pnpm run build:tsc && pnpm version patch && pnpm publish",
    "minor": "pnpm run build:tsc && pnpm version minor && pnpm publish",
    "major": "pnpm run build:tsc && pnpm version major && pnpm publish",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "eslint": "^8.20.0",
    "vite": "^3.0.2",
    "vitest": "^0.18.1"
  },
  "devDependencies": {
    "c8": "^7.12.0",
    "esbuild": "^0.14.49",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  },
  "prettier": {
    "singleQuote": true,
    "bracketSameLine": true
  }
}
